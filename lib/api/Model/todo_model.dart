class todoModel {
  int userId;
  int id;
  String title;
  bool completed;
  todoModel({
    required this.userId,
    required this.id,
    required this.title,
    required this.completed,
  });
  
}
